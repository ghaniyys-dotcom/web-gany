@extends('admin.layout')@section('heading','Message Detail')@section('content')<div class="card"><p><span class="muted">Nama:</span> <b>{{ $message->name }}</b></p><p><span class="muted">Email:</span> <a href="mailto:{{ $message->email }}">{{ $message->email }}</a></p><p><span class="muted">Company:</span> {{ $message->company ?: '-' }}</p><p><span class="muted">Budget:</span> {{ $message->budget ?: '-' }}</p><p><span class="muted">Tanggal:</span> {{ $message->created_at->format('d M Y H:i') }}</p><hr style="border-color:#24304f;border-style:solid;border-width:1px 0 0;margin:20px 0"><p style="white-space:pre-wrap;font-size:18px;line-height:1.6">{{ $message->message }}</p><div style="display:flex;gap:10px;flex-wrap:wrap"><a class="btn" href="mailto:{{ $message->email }}?subject={{ urlencode('Re: '.$message->subject) }}">Reply Email</a><form method="POST" action="{{ route('admin.messages.destroy',$message) }}" onsubmit="return confirm('Hapus pesan ini?')">@csrf @method('DELETE')<button class="btn danger">Hapus</button></form></div></div>@endsection